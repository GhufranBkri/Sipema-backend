openapi: 3.0.0
info:
  title: Sipema API
  version: "1.0.0"
  description: API Documentation untuk Sistem Penjaminan Mutu (SIPEMA)

servers:
  - url: http://localhost:3150
    description: Development server

paths:
  /health:
    get:
      summary: Health check endpoint
      responses:
        "200":
          description: Server is running
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "ok"
